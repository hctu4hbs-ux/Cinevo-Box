# 🛠️ دليل المطورين - Cinevo Box

## مرحباً بك في فريق التطوير!

هذا الدليل يساعدك على فهم المشروع والبدء بالمساهمة.

---

## 📋 المتطلبات

### الأساسيات
- متصفح ويب حديث
- محرر نصوص (VS Code مفضل)
- Git (اختياري للمساهمة)

### اختياري
- Node.js (للأدوات المتقدمة)
- Python (لخادم تطوير محلي)

---

## 🏗️ البنية المعمارية

```
Cinevo-Box/
├── index.html          # نقطة الدخول الرئيسية
├── css/
│   └── style.css       # جميع الأنماط (متجاوب)
├── js/
│   ├── api.js          # طبقة TMDB API
│   ├── app.js          # منطق التطبيق الرئيسي
│   └── player.js       # مشغل الفيديو والترجمات
└── images/             # الصور والموارد
```

---

## 🔄 تدفق البيانات

```
UI (HTML) 
   ↓
JavaScript (app.js)
   ↓
API Layer (api.js)
   ↓
TMDB API
   ↓
JSON Data
   ↓
Formatting & Display
   ↓
Styled with CSS
```

---

## 📝 الملفات الرئيسية

### index.html
- **الغرض**: الهيكل الأساسي
- **الدور**: تعريف العناصر والأقسام
- **الملاحظات**: يحتوي على جميع الأقسام ومتغيرات الحالة

### js/api.js
- **الغرض**: تكامل TMDB API
- **الوظائف الرئيسية**:
  - `fetchFromTMDB()` - طلب عام للـ API
  - `fetchPopularMovies()` - الأفلام الشهيرة
  - `searchContent()` - البحث
  - `getCompleteMovieData()` - تفاصيل الفيلم
  - `generateStreamingLinks()` - توليد روابط البث

### js/app.js
- **الغرض**: منطق التطبيق الرئيسي
- **المسؤولية عن**:
  - إدارة الحالة (State)
  - التعامل مع الأحداث (Events)
  - عرض البيانات على الواجهة
  - التنقل بين الأقسام

### js/player.js
- **الغرض**: مشغل الفيديو والترجمات
- **المكونات**:
  - `VideoPlayer` - تحكم الفيديو
  - `SubtitleManager` - إدارة الترجمات
  - `StreamingSourceManager` - مصادر البث
  - `Analytics` - تحليل المشاهدة

### css/style.css
- **الغرض**: التصميم كامل
- **المتضمن**:
  - المتغيرات (CSS Variables)
  - الأنماط العام
  - الاستجابة (Media Queries)
  - الرسوم المتحركة

---

## 🚀 البدء بالتطوير

### 1. الإعداد المحلي

```bash
# استنساخ المشروع
git clone https://github.com/yourusername/Cinevo-Box.git
cd Cinevo-Box

# فتح في VS Code
code .

# تشغيل خادم محلي
python -m http.server 8000
```

### 2. فتح المتصفح

اذهب إلى: `http://localhost:8000`

### 3. فتح أدوات المطور

- Windows/Linux: `F12`
- Mac: `Cmd + Option + I`

---

## 🔧 التعديلات الشائعة

### تغيير ألوان الموضوع

في `css/style.css`:
```css
:root {
    --primary-color: #1a1a1a;      /* الأسود الأساسي */
    --secondary-color: #e50914;    /* الأحمر الأساسي */
    --text-color: #ffffff;         /* اللون النصي */
    /* ... الخ ... */
}
```

### إضافة فئة جديدة

في `html` أضف:
```html
<button onclick="loadMoviesByGenre('YOUR_GENRE_ID')">اسم الفئة</button>
```

ثم في `js/api.js` أضف المعرّف:
```javascript
const GENRE_NAMES = {
    // ... موجود ...
    YOUR_GENRE_ID: 'اسم الفئة العربي',
};
```

### تخصيص مصادر البث

في `js/api.js`:
```javascript
const STREAMING_SOURCES = {
    // أضف مصدر جديد
    newsource: 'https://newsource.com/embed/',
};
```

### تغيير اللغة

تم بناء الموقع بالعربية بشكل كامل. لتغيير اللغة، عدّل:
- النصوص في `index.html`
- الرسائل في `js/app.js`
- البيانات من TMDB API

---

## 🐛 استكشاف الأخطاء

### أداة المطور (Developer Tools)

```javascript
// في Console، يمكنك:

// اختبار API
fetchPopularMovies().then(data => console.log(data))

// عرض الحالة الحالية
console.log('الأفلام:', currentMovies)
console.log('المفضلة:', favorites)

// اختبار دالة
showDetails(550, 'movie') // عرض تفاصيل Fight Club

// مسح البيانات المخزنة
localStorage.clear()
```

### رسائل الأخطاء الشائعة

```
CORS Error → تحقق من سياسة CORS في المتصفح

API Key Invalid → تحقق من مفتاح TMDB

Network Error → تحقق من الاتصال بالإنترنت

Undefined Variable → ابحث عن التهجئة الخاطئة
```

---

## 📚 دليل الأنماط (Style Guide)

### تسمية المتغيرات
```javascript
// جيد
const currentMovies = []
let isLoading = false

// سيء
const movies = []
let loading = false
```

### تسمية الدوال
```javascript
// جيد
function loadMoviesGrid() {}
function handleSearch() {}

// سيء
function load() {}
function search() {}
```

### التعليقات
```javascript
// تعليق مختصر
const API_KEY = 'xxx' // مفتاح TMDB

// تعليق شامل
/**
 * تحميل الأفلام من TMDB
 * @param {number} page - رقم الصفحة
 * @returns {Promise<Array>} قائمة الأفلام
 */
async function fetchMovies(page = 1) {}
```

---

## 🧪 الاختبار

### اختبر في المتصفح

```javascript
// في Console

// 1. اختبر البحث
document.getElementById('searchInput').value = 'Avatar'
performSearch()

// 2. اختبر التفاصيل
showDetails(19995, 'movie') // Avatar

// 3. اختبر الفلاتر
document.getElementById('genreFilter').value = '28'
applyFilters()

// 4. اختبر المفضلة
toggleFavorite()
console.log('المفضلة:', favorites)
```

### اختبر على أجهزة مختلفة

1. استخدم الوضع المتجاوب (Responsive Mode) في F12
2. اختبر على هاتف حقيقي عبر الشبكة المحلية
3. اختبر على أحجام شاشة مختلفة

---

## 📈 الأداء

### نصائح التحسين

1. **تقليل الطلبات**
   ```javascript
   // استخدم التخزين المؤقت
   if (cache.movies.length > 0) {
       return cache.movies
   }
   ```

2. **تحسين الصور**
   - استخدم الصور المناسبة للحجم
   - أضف صور بديلة (placeholder)
   - استخدم lazy loading

3. **تحسين CSS**
   - استخدم CSS Variables
   - قلل القواعد المتكررة
   - استخدم Flexbox و Grid

4. **تحسين JavaScript**
   - استخدم البحث الفعال
   - قلل الحلقات المتداخلة
   - استخدم Async/Await

---

## 🚢 قائمة المراجعة قبل الإصدار

- [ ] اختبر جميع الوظائف
- [ ] تحقق من عدم وجود أخطاء في Console
- [ ] اختبر على أجهزة مختلفة
- [ ] تحقق من الأداء
- [ ] حدّث التوثيق
- [ ] اختبر الرسائل والإشعارات
- [ ] تحقق من الترجمات
- [ ] تحقق من الروابط الخارجية

---

## 🤝 المساهمة

### خطوات المساهمة

1. **Fork** المشروع
2. **أنشئ branch** جديد:
   ```bash
   git checkout -b feature/new-feature
   ```
3. **عدّل الملفات**
4. **Commit** التعديلات:
   ```bash
   git commit -m "إضافة ميزة جديدة"
   ```
5. **Push** إلى GitHub:
   ```bash
   git push origin feature/new-feature
   ```
6. **أنشئ Pull Request**

### معايير الكود

- اتبع أسلوب الكود الموجود
- أضف تعليقات واضحة
- اختبر التعديلات
- حدّث التوثيق

---

## 📚 مراجع مفيدة

### TMDB API
- [التوثيق الرسمي](https://www.themoviedb.org/settings/api)
- [استكشاف API](https://www.themoviedb.org/talk/category/5047951f760ee3261f004562)

### JavaScript
- [MDN Web Docs](https://developer.mozilla.org/)
- [JavaScript.info](https://javascript.info/)

### CSS
- [CSS-Tricks](https://css-tricks.com/)
- [Flexbox Guide](https://css-tricks.com/snippets/css/a-guide-to-flexbox/)

---

## 🎓 الموارد التعليمية

### فيديوهات مفيدة
- TMDB API Tutorial
- JavaScript Async/Await
- CSS Grid و Flexbox
- Video Player Integration

### مقالات
- Best Practices في JavaScript
- API Integration Patterns
- Responsive Web Design

---

## 💬 التواصل والدعم

### الأسئلة الشائعة

**س: كيف أضيف مشغل فيديو جديد؟**
ج: عدّل `js/player.js` وأضف الوظائف المطلوبة

**س: كيف أغير ألوان الموقع؟**
ج: عدّل `--primary-color` و `--secondary-color` في CSS

**س: هل يمكن إضافة لغات أخرى؟**
ج: نعم، عدّل النصوص وغيّر `lang` في HTML

---

## 📝 نموذج المشكلة

عند الإبلاغ عن خطأ، أرسل:

```
العنوان: [BUG] وصف مختصر

الوصف:
- ما الذي حدث؟
- ما المتوقع؟
- الخطوات للتكرار:
  1. ...
  2. ...
  3. ...

البيئة:
- المتصفح: ...
- الجهاز: ...
- رابط: ...
```

---

## 🎯 الأهداف المستقبلية

### قريباً
- [ ] المزيد من مصادر البث
- [ ] نظام التقييمات
- [ ] قوائم مخصصة

### بعيداً
- [ ] تطبيق جوال
- [ ] نظام الحسابات
- [ ] توصيات ذكية

---

**شكراً على مساهمتك!** 🙏

---

**آخر تحديث**: يناير 2026  
**الإصدار**: 1.0.0
